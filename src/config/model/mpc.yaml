name: mpc

cplexpath: "/home/user/D_Disk/gjc/cplex/cplex/opl/bin/x86-64_linux/" # Set to null/None to use the PuLP macro fallback

no_cuda: true  # Disables CUDA training

test_episodes: 10 # Number of episodes to test agent

oracle: true  # Use oracle for MPC or forecast

directory: "saved_files"

wandb: false
